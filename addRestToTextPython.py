# Python code for checking the participant files and adding a rest...
# condition to the BEGINING only (may update to add in where missing elsewhere)
class addrest:
    def __init__(self, fName,foPath):
        fCur = open(self)
fList = os.listdir(path)

currentFile = open("")
foPath = "/Users/user/Downloads/Summer Training"
